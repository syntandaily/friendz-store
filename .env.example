# Server Configuration
PORT=5000
JWT_SECRET=friendz-super-secret-jwt-key-2026

# Database Type: 'sqlite' or 'mysql'
DB_TYPE=sqlite

# MySQL Configuration (Used if DB_TYPE=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=friendz_db

# SQLite Configuration (Used if DB_TYPE=sqlite)
SQLITE_PATH=database/friendz.db
